"""The golden set: the cases Phase 0 measured, kept runnable so regressions show up.

`make eval` runs every case through the real pipeline and prints a quality table.
The scorers live in `editgpt_core.metrics`; this package owns the fixtures and the
harness around them.
"""
